{
  "name": "motion-comic-studio-home",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy:gh-pages": "npm run build && touch dist/.nojekyll && npx --yes gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
